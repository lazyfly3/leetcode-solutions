# 台式機與筆記本電腦同步設置指南

## 方案一：使用 Git + GitHub/GitLab（推薦）

這是最適合代碼項目的同步方式，可以追蹤所有更改歷史。

### 步驟 1：在台式機上設置遠程倉庫

1. **創建 GitHub 或 GitLab 帳號**（如果還沒有的話）
   - GitHub: https://github.com
   - GitLab: https://gitlab.com

2. **在 GitHub/GitLab 上創建新倉庫**
   - 倉庫名稱建議：`leetcode-solutions`
   - 設置為 Private（私有）以保護你的代碼

3. **在台式機上連接遠程倉庫**
   ```bash
   cd d:\postgraduate\leetcode
   git remote add origin https://github.com/你的用戶名/leetcode-solutions.git
   git branch -M main
   git push -u origin main
   ```

### 步驟 2：在筆記本電腦上克隆倉庫

1. **安裝 Git**（如果還沒安裝）
   - 下載：https://git-scm.com/download/win

2. **克隆倉庫到筆記本電腦**
   ```bash
   cd 你想要的位置
   git clone https://github.com/你的用戶名/leetcode-solutions.git
   cd leetcode-solutions
   ```

### 步驟 3：日常同步流程

#### 在台式機上修改後：
```bash
cd d:\postgraduate\leetcode
git add .
git commit -m "描述你的更改"
git push
```

#### 在筆記本電腦上修改後：
```bash
cd 你的倉庫路徑
git add .
git commit -m "描述你的更改"
git push
```

#### 在另一台機器上獲取最新更改：
```bash
cd 你的倉庫路徑
git pull
```

---

## 方案二：使用 OneDrive/Google Drive（簡單但有限制）

如果你不想使用 Git，可以使用雲存儲服務。

### OneDrive 設置：
1. 將 `d:\postgraduate\leetcode` 移動到 OneDrive 文件夾
2. 在兩台電腦上都登錄同一個 OneDrive 帳號
3. 文件會自動同步

**注意**：這種方式不適合大型項目，且無法處理合併衝突。

---

## 方案三：使用網絡共享（僅限同一網絡）

如果兩台電腦在同一網絡下：
1. 在台式機上共享 `d:\postgraduate\leetcode` 文件夾
2. 在筆記本電腦上映射網絡驅動器
3. 直接編輯網絡文件

**注意**：需要兩台電腦同時在線，且在同一網絡。

---

## 推薦使用方案一（Git）

Git 的優點：
- ✅ 完整的更改歷史
- ✅ 可以處理合併衝突
- ✅ 即使沒有網絡也可以工作（本地提交）
- ✅ 專業開發者標準工具
- ✅ 可以回退到任何歷史版本

---

## 快速開始命令（Git 方案）

### 首次設置（在台式機上）：
```bash
cd d:\postgraduate\leetcode
git remote add origin https://github.com/你的用戶名/倉庫名.git
git push -u origin main
```

### 在筆記本電腦上：
```bash
git clone https://github.com/你的用戶名/倉庫名.git
```

### 日常使用：
- **提交更改**：`git add .` → `git commit -m "說明"` → `git push`
- **獲取更新**：`git pull`

